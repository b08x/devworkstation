---
# TODO (P0, correctness) c15/c16: only `theme` is defined here, but
# tasks/main.yml loops desktop_packages.qt and .gtk and gnome.yml expects
# .gnome. On Fedora those raise "'dict object' has no attribute 'qt'", the
# surrounding rescue swallows it, and the run reports green having installed
# no Qt, GTK or GNOME packages. Add the missing keys or gate the loops.
desktop_packages:
  theme:
    - gnome-shell-theme-yaru
    - yaru-gtk2-theme
    - yaru-gtk3-theme
    - yaru-gtk4-theme
    - yaru-gtksourceview-theme
    - yaru-icon-theme
    - yaru-sound-theme
    - yaru-theme
