---
# argument spec file for b08x.devworkstation.selinux

argument_specs:
  main:
    short_description: Role description.
    options:
      selinux_my_variable:
        type: str
        description: A simple string argument for demonstration.
        default: "default_value"
